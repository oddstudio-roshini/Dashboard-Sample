package com.medicare.config;

// CorsConfig intentionally disabled — CORS is handled solely by SecurityConfig
// to prevent duplicate headers that cause browser rejections.
public class CorsConfig {
}
